# ── 서버 / 웹 ────────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.6

# ── 영상 / AI ────────────────────────────────────────────────
opencv-python>=4.9.0
mediapipe==0.10.13
numpy>=1.24.0
torch>=2.0.0          # CPU 버전: pip install torch --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.15.0   # ResNet 모델에 필요
Pillow>=10.0.0

# ── 집중도 분류 모델 ──────────────────────────────────────────
pandas>=2.0.0
joblib>=1.3.0
scikit-learn>=1.3.0

# ── 음성 인식 ─────────────────────────────────────────────────
SpeechRecognition>=3.10.0
PyAudio>=0.2.14       # Windows 설치 실패 시: pip install pipwin && pipwin install pyaudio

# ── Windows 전용 (태블릿 화면 분석) ──────────────────────────
pywin32>=306
winrt-Windows.Media.Ocr>=2.0
winrt-Windows.Graphics.Imaging>=2.0
winrt-Windows.Storage.Streams>=2.0
winrt-Windows.Globalization>=2.0
winrt-Windows.Foundation>=2.0

# ── 의미 기반 OCR 분류 (없으면 키워드 룰로 자동 폴백) ──────────
sentence-transformers>=2.7.0,<4.0.0
